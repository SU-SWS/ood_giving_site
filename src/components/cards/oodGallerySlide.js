import React from 'react'
import SbEditable from 'storyblok-react'

const oodGallerySlide = (props) => {
  return (
    <SbEditable content={props.blok}>
      <div>Slide test</div>
    </SbEditable>
  )
}

export default oodGallerySlide